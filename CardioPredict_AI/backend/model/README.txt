Place your trained model files here:
- cardio_model.h5   (Keras/TensorFlow model)
- scaler.pkl        (fitted StandardScaler)

These files are excluded from GitHub via .gitignore because they are too large.
Download them separately and place them in this folder before running the backend.
